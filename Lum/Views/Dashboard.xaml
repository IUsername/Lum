<Page
    x:Class="Lum.Views.Dashboard"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Lum.Views"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:controls="using:Microsoft.Toolkit.Uwp.UI.Controls"
    xmlns:lum="using:Lum"
    mc:Ignorable="d">

    <Grid>
        <controls:WrapPanel Grid.Row="0" Orientation="Horizontal" HorizontalAlignment="Center"
                            VerticalAlignment="Center"
                            HorizontalSpacing="60" VerticalSpacing="40" Margin="20">

            <lum:RadialIndicator Value="{Binding CPU}" MaxWidth="400" Title="CPU" />
            <lum:RadialIndicator Value="{Binding GPU}" MaxWidth="400" Title="GPU" />
            <lum:RadialIndicator Value="{Binding Memory}" MaxWidth="400" Title="Memory" />

            <Grid Background="#FF383838">
                <ToggleSwitch Header="Performance Boost" Margin="20" />
            </Grid>

        </controls:WrapPanel>

    </Grid>

</Page>